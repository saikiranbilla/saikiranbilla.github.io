# saikiranbilla.me

Personal portfolio. Static HTML + vanilla JS, deployed to GitHub Pages.

## Content

Site content is authored directly in `index.html`. Project data lives in `data.json` for reference.

## Deploy

Pushes to `main` deploy to GitHub Pages via `.github/workflows/deploy.yml`.

Custom domain `saikiranbilla.me` is configured via `CNAME`.
